import React from "react";
import ReactDOM from "react-dom/client";
import SOM3ALanding from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
    <React.StrictMode>
        <SOM3ALanding />
    </React.StrictMode>
);
